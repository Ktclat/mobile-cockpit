# E1.5 implementer report

Base commit: `ab33ffb`

## TDD evidence

- Initial required command, `./gradlew :presentation:testDebugUnitTest --tests '*CockpitShellTest'`, failed because the Android unit-test task did not yet exist. The Android test harness was then added without production shell code.
- Valid RED: after the harness and verified dependencies were available, `./gradlew :presentation:testDebugUnitTest --tests '*CockpitShellTest'` reached `:presentation:compileDebugUnitTestKotlin` and failed only on unresolved `CockpitProcessComponent` and `CockpitRoot` references from `CockpitShellTest`.
- Green: the final required command, `./gradlew :presentation:testDebugUnitTest :app:assembleDebug`, completed successfully. The local XML result reports `CockpitShellTest.displaysAppNameThroughCompositionRoot`: 1 test, 0 failures, 0 errors.

## Dependencies and verification

- Android configuration: minSdk 28, compileSdk/targetSdk 36, Java 17.
- Direct additions: Compose BOM `2026.06.00`; Compose UI/Foundation/UI test `1.11.4`; Activity Compose `1.13.0`; AndroidX Test JUnit `1.3.0`; JUnit `4.13.2`; Robolectric `4.16.1` (test SDK 35).
- All dependency verification metadata was generated with Gradle `--write-verification-metadata sha256`; strict verification remained enabled. The metadata diff is `+3255/-293` lines relative to the base after final resolution.
- A prior brief mapping to Compose 1.12.0 was corrected by the controller because it requires compileSdk 37. Gradle verification metadata retains additive stale 1.12.0 checksum entries from that discarded resolution; the final catalog and resolved build use only Compose 1.11.4. No metadata was hand-authored or verification policy weakened.

## Artifact and focused verification

- `app/build/outputs/apk/debug/app-debug.apk` exists and is non-empty (23,443,293 bytes).
- The merged debug manifest has package `dev.cockpit.mobile`, minSdk 28, targetSdk 36, `dev.cockpit.mobile.CockpitApplication`, visible label `Cockpit`, and exactly one exported `dev.cockpit.mobile.MainActivity` with MAIN/LAUNCHER. Unused transitive AndroidX provider, receiver, permissions, and optional window library declarations are removed from the packaged empty shell.
- `./gradlew :architecture-tests:test --tests '*VersionCatalogPolicyTest'` passed: 3 tests, 0 failures/errors.
- `./gradlew :architecture-tests:test --tests '*ModuleGraphTest*'` passed: 4 tests, 0 failures/errors. Its self-mutation hook now targets `com.android.library`, matching Presentation's required Android-module conversion and still detects an injected forbidden production edge.

## Self-review

- `git diff --check` passed before commit.
- Reviewed all E1.5 changes against `ab33ffb`: app remains packaging/entry points only; production Presentation has no Platform dependency; its Platform dependency is test-only; `gradle/module-graph.txt` is unchanged; Platform owns the sole process component; the Compose root renders only the composed app-name text.
- No E1.6, product UI, navigation, persistence, runtime, provider, SSH, Vault, permissions, services, or background behavior was added.

Commit: `build: launch empty cockpit shell` (the resulting SHA is recorded in the implementation handoff).
